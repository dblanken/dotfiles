tags
nvim/.config/nvim/plugin
git/.local

# macOS
.DS_Store
.AppleDouble
.LSOverride

# Local config files (but not .local directories like scripts/.local/)
**/*.local
.zprofile.local*
!scripts/.local/
