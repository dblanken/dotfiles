function ruby_version() {
  symbol="💎 "

  [[ -f Gemfile || -f Rakefile ]] || return

  ruby_version=${$(asdf current ruby)[2]}
  if [[ ${#ruby_version} > 0 ]]; then
    echo "%F{cyan}[${symbol}%F{green}${ruby_version}%F{cyan}]%f"
  fi
}
