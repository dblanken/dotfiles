function node_version() {
  symbol="⬢ "

  [[ -f package.json || -d node_modules ]] || return

  node_version=${$(asdf current nodejs)[2]}
  if [[ ${#node_version} > 0 ]]; then
    echo "%F{cyan}[${symbol}%F{green}${node_version}%F{cyan}]%f"
  fi
}

