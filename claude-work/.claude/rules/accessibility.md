---
description: WCAG 2.1 A/AA accessibility requirements for UI work
paths:
  - "**/*.{tsx,jsx,vue,svelte}"
  - "**/*.{css,scss,sass,less}"
  - "**/*.{twig,html}"
---

# Accessibility Standards

- **Minimum**: WCAG 2.1 A and AA — always apply when implementing or fixing UI
- See `~/.claude/memory/wcag-checklist.md` for the full checklist when building or
  reviewing UI
